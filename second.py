#this is a second py file to test the merge of 2 repos
def looper(number):
	for i in range(0,20):
		print('This is ' + str(i) + ' times.')
	print('Looper is finished now.')

numberEntered = input('Enter a number : ')
looper(numberEntered)
# looper adding a comment to the last line

